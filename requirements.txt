# Required to build the model
openpyxl==3.1.5

# Optional - only needed to run src/verify_model.py, which recalculates every
# formula in the generated workbook without Excel and checks the bridge ties.
# formulas==1.3.4
